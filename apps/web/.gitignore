.vercel
.env*

# Saída do Playwright (traces/screenshots de falha) - artefato local, nunca versionado.
test-results
playwright-report

# Screenshots e PNGs das baterias E2E do Canva (Gate 2.3): artefato de execução,
# nao codigo. Nunca versionar - pode conter dado de cliente de teste.
tests/e2e/artifacts
