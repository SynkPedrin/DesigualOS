# Seeds

Os dados de seed (agentes, papéis, matriz de permissões do Tool Gateway) ficam em código, em `packages/database/src/seed.ts`, executado via `pnpm db:seed`. Esta pasta existe para eventuais arquivos de dados brutos (JSON, CSV) que um seed maior venha a precisar em fases futuras.
